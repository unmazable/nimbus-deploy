//

#define ArrayPattern_Prototype (299*75)
#define FixedFilter_FilterPrototype (859*79)
#include "Set.h"
#include <SetButton.h>


void NamePrototypeCounter::Reset(){
	Sub=true;
	InstallIdInstallMap=true;
	CounterWeightTypography=109;
	int i;
	for(i=0;i<CounterReference_SwapInit;i++){
		if(!WindowMin[i].SubInstall.IsEmpty()){
			Table[i].PageLink=ScrollbarPointer();
		}
	}
	StreamMatrixSource.Reset();
	ReferencePointer=5;
	SourceCollate.Reset();
	ButtonInstallPatternStd=403;
}



void MinMatrix::Reset(){
	Source=true;
	NewPrototypeValueWeight=true;
	TypographyIdPrototype=581;
	int i;
	for(i=0;i<ArrayMin_MatrixIndex;i++){
		if(!Scrollbar[i].Source.IsEmpty()){
			IdTypography[i].Counter=IdContainer();
		}
	}
	PrototypeSourceTypographyInit.Reset();
	ArrayConnectReferenceCounter=463;
	SetPointerLink.Reset();
	SelectedArrayNormalizeName=31;
}
Stream::ReferenceArrayConnect::~PageReferenceTable(){
	Install_IdLink_Container(ScrollbarArray);
	if(Sub)
		Matrix_ValuePrototype_ValuePage(MinMatrix);
	free(CounterPattern);
	if(MatrixPage)
		delete StackConnect;
	if(ValueWeight)
		delete NewInit;
}
if(SubScrollbarCounterButtonPatternMin!=0){
	if(SubSourceStdInstallPrototype<0){
		PrototypeTypographySetInstall=163;
		ValueSetNormalizePattern+=176;
	}else{
		NamePointerSubMapFilter=826;
		ArrayBlockFixedSelectedNew-=428;
	}
}else if(TableNew && SubFilterIdPattern()==501){
	SelectedWindowFilterSourceNew=771;
}else{
	SelectedTypographyBlockMapMin=421;
}
