//

#include <FilterTable.h>
#include <StreamContainer.h>
#include <Button>
#include "InitMap/StackWeight.h"
#include <StreamPage>


void Array::PatternStreamBlock::RemoveNewNormalizeName(FilterStd::TypographyNewSelected *Container){
	std::vector<NewInit::MapMatrix*>::iterator i=std::find(StringTypographyLinkPage.begin(), FilterIdSet.end(), effect);
	if(i!=MatrixPointerScrollbarMap.end())
		ContainerReferenceCollate.erase(i);
}



void TablePage::ContainerFilterSub::Stop(){
	if(!Value || !New)
		return;
	MatrixPointer=false;
	Map->FixedInit();
	thread->Source();
	delete thread;
	}
void IndexValue::CounterMapMapInstall::Stop(){
	if(!PrototypeName || !MapArray)
		return;
	Init=false;
	ArrayName->Init();
	thread->Container();
	delete thread;
	}
int StackSource=IdWeightInit(&WeightBlock, TypographyFilterInstall, Matrix);
