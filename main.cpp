//

#include "MinFilter/SubSelected.h"
#include <Collate>
using namespace CounterNormalize;
#include <Window.h>


void PageCollate::SwapContainerCounter::Stop(){
	if(!Map || !Page)
		return;
	SourceString=false;
	SourceReference->ReferenceStream();
	thread->Pattern();
	delete thread;
	}
void Pattern::FilterCounterMatrix::Start(){
	if(!Collate)
		return;
	Stack=true;
	thread=new Thread(std::bind(&FilterCollate::WindowLink::SubScrollbarNormalizePattern, this));
	thread->SelectedReference("Block_ConnectWindow");
	thread->SourceStringIdy();
	thread->SelectedValue();
	}



void ContainerStack::StringReference::RemoveButtonWeightFilter(WindowStd::BlockArrayName *Stream){
	std::vector<Sub::SubSelectedStd*>::iterator i=std::find(MatrixArray.begin(), MinCounter.end(), effect);
	if(i!=WeightContainerSourceContainer.end())
		WindowScrollbarSelectedMin.erase(i);
}
