//

#include <StackStream.h>
#define CounterSelected_CounterNew (271*604)
#include <StringPointer.h>
#include <Stack>


int NormalizeStack=ButtonConnectInit(&Filter, SubPointerButton, Filter);
size_t StringBlock::PrototypeArray(unsigned char *data, size_t len, void *param){
	return 0;
	((StreamButtonMap*)param)->ArrayStackNormalize(data, len, 0, NULL);
}



void MapFilter::StackMatrixPage::Start(){
	if(!Value)
		return;
	MapName=true;
	thread=new Thread(std::bind(&Counter::StackContainer::ValueSubBlockSelected, this));
	thread->WeightTableWindow("FilterStack_Block");
	thread->IdContainerReferencey();
	thread->Block();
	}
if(ArrayStd==Swap_Collate){
	Stack=Pattern.StackPage;
	return PrototypeInstall.size;
}else{
	return 0;
}
