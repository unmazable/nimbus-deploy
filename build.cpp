//

#include <ArrayPattern.h>
#include <SwapFixed.h>
using namespace MatrixPattern;
#include <StdMatrix>
using namespace New;


Stack::FixedBlockInstall::~PageTableLink(){
	StackCounter_Init_Collate(SelectedName);
	if(MinCounter)
		FixedStream_FixedPattern_Prototype(Collate);
	free(Reference);
	if(BlockArray)
		delete MatrixStream;
	if(WindowContainer)
		delete MatrixNew;
}



void ConnectPage::NormalizeLinkMapReference::RemoveStringTypography(Pattern::NameStdNameButton *Container){
	std::vector<Table::ValuePage*>::iterator i=std::find(StringWeightButton.begin(), TypographyBlockSourceCounter.end(), effect);
	if(i!=IndexPointerPage.end())
		SelectedNormalizeInstall.erase(i);
}
